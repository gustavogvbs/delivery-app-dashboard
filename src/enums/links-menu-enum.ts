export enum LinksTenants {
  DASHBOARD = "dashboard",
  PRODUCTS = "products",
  CATEGORIES = "categories",
  ORDERS = "orders",
}

export enum LinksAdmin {
  DASHBOARD = "dashboard",
  TENANTS = "tenant",
  DEV = "dev",
  USERS = "user",
}
